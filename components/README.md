# Components
Reusable semantic component patterns are demonstrated in `index.html` and the page templates. The structure can later be migrated to a component framework without changing the semantic DOM patterns.
